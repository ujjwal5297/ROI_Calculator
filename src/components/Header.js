import { Typography } from "@mui/material";

import React from 'react'

const Header = () => {
  return (
    <Typography variant='h3'sx={{fontWeight: 'bold', color: 'GrayText'}}>
        ROI Calculator
    </Typography>
  )
}

export default Header